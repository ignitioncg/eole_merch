# monday.com personal API token (Admin → Developer → My access tokens)
MONDAY_API_TOKEN=

# Shared secret used to sign monday webhook requests (set when registering the webhook)
MONDAY_WEBHOOK_SECRET=

# Stock count below which an item is marked Low Stock (default 25)
LOW_STOCK_THRESHOLD=25

# HTTP port for the webhook server
PORT=3000
