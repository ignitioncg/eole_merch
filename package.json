{
  "name": "eole-stock-catalog",
  "version": "1.0.0",
  "description": "Private monday.com app for End of Life Essentials: Custom Object portal for merchandise stock and orders, with bidirectional sync to existing boards and a Stock Movements audit board.",
  "private": true,
  "main": "index.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "node --test 'test/*.test.js'",
    "build:client": "npm install --prefix client && npm run build --prefix client",
    "dev:client": "npm install --prefix client && npm run dev --prefix client",
    "dryrun": "node scripts/dryrun.js",
    "deploy:client": "npm run build:client && cd client && mapps code:push -c -d dist",
    "deploy:server": "mapps code:push"
  },
  "dependencies": {
    "@mondaycom/apps-sdk": "3.0.10",
    "express": "^4.19.2",
    "ulid": "^2.3.0"
  },
  "overrides": {
    "@tootallnate/once": "^3.0.1",
    "uuid": "^14.0.0",
    "gaxios": "^7.1.4",
    "http-proxy-agent": "^9.0.0",
    "retry-request": "^8.0.2",
    "teeny-request": "^10.1.2"
  }
}
